body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background-color: black;
    color: white;
    font-family: Arial, sans-serif;
    margin: 0;
}

.container {
    text-align: center;
}

h1 {
    font-size: 24px;
    color: white;
    margin-bottom: 20px;
}

.vote-buttons {
    display: flex;
    align-items: center;
    gap: 20px;
    font-size: 24px;
}

button {
    padding: 10px;
    cursor: pointer;
    border: none;
    background: none;
    color: white;
    font-size: 18px;
}

.upvote {
    background-color: #4a90e2; /* Blue color for upvote */
}

.downvote {
    background-color: #e94e36; /* Red color for downvote */
}

#upvoteCount, #downvoteCount {
    font-size: 24px;
    color: white;
}

.image-container {
    margin-top: 30px;
}

img {
    width: 200px; /* Starting width */
    height: auto;
    transition: width 0.3s ease-in-out; /* Smooth transition for resizing */
}
